---
const { heading, content, subHeading } = Astro.props;

interface Props {
  heading?: string;
  content?: string;
  subHeading?: string;
}

const headingClasses =
  "text-balance text-lg font-bold text-neutral-800 dark:text-neutral-200";
const contentClasses =
  "mt-1 text-pretty text-neutral-700 dark:text-neutral-300";
---

<div class="flex gap-x-5">
  <slot />
  <div class="grow">
    <h3 class={headingClasses}>
      {heading}
    </h3>
    {
      subHeading && (
        <h2 class="text-neutral-600">
          <p class="inline-flex">{subHeading}</p>
        </h2>
      )
    }
    <p class={contentClasses}>{content}</p>
  </div>
</div>
