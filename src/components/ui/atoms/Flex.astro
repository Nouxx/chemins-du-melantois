---
import type { HTMLTag } from "astro/types";
import { cx } from "class-variance-authority";

interface Props {
  direction?: "col" | "row";
  gap?: number;
  className?: string;
  renderAs?: HTMLTag;
}

const { direction, gap, className = "", renderAs } = Astro.props;

const directionClass = direction ? `flex-${direction}` : undefined;
const gapClass = gap ? `gap-${gap}` : undefined;

const mergedClasses = cx(directionClass, gapClass, className, "flex");

const Tag = renderAs ?? "div";
---

<Tag class={mergedClasses}>
  <slot />
</Tag>
