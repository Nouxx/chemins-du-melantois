---
import { cx } from "class-variance-authority";

interface Props {
  direction?: "col" | "row";
  gap?: number;
  className?: string;
}

const { direction, gap = 0, className = "" } = Astro.props;

const directionClass = direction ? `flex-${direction}` : undefined;
const gapClass = gap ? `gap-${gap}` : undefined;

const mergedClasses = cx(directionClass, gapClass, className, "flex");
---

<div class={mergedClasses}>
  <slot />
</div>
