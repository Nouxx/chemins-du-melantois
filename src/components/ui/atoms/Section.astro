---
import { cva, cx, type VariantProps } from "class-variance-authority";

interface Props {
  flex?: Variants["flex"];
  className?: string;
}

const { flex, className } = Astro.props;

const section = cva(
  [
    "items-stretch",
    "mx-auto",
    "max-w-[85rem]",
    "2xl:max-w-full",
    "px-4",
    "sm:px-6",
    "py-14",
    "gap-4",
  ],
  {
    variants: {
      flex: {
        col: "flex flex-col",
        row: "flex flex-row",
        none: undefined,
      },
    },
    defaultVariants: { flex: "none" },
  }
);

type Variants = VariantProps<typeof section>;
---

<section class={cx(section({ flex }), className)}>
  <slot />
</section>
