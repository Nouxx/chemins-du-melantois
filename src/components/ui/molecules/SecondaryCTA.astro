---
import LinkButton from "@components/ui/atoms/LinkButton.astro";

interface Props {
  title: string;
  url: string;
}

const { title, url } = Astro.props;
---

<LinkButton
  label={title}
  url={url}
  target="_blank"
  color="neutral"
  fontStyle="medium"
  disabled={false}
/>
