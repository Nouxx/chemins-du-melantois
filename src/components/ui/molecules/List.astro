---
interface Props {
  className?: string;
  spaceY?: number;
}

// todo: use cva

const { className, spaceY } = Astro.props;
---

<ul class={`space-y-${spaceY || 2}`} class={className}>
  <slot />
</ul>
